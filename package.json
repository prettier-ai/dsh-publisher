{
  "name": "dsh-publisher",
  "version": "0.0.0",
  "private": true,
  "description": "Standalone publisher that republishes official DeepSeek Harness releases under the @prettier-ai npm scope.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10.33.3",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json",
    "probe": "node scripts/probe-upstream-release.ts"
  },
  "engines": {
    "node": ">=22.6"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
